# HotWire — real-hardware-only Python dependencies.
#
# This file is NOT required for the AEC Functional badge or for any
# simulation-mode work. It is only needed if you want to run HotWire
# against a real QCA7005 PLC modem (the `--hw` paths and
# `scripts/hw_check/phase[1-9]_*.py`).
#
# System-package prerequisite (Debian / Ubuntu / Raspberry Pi OS):
#     sudo apt install -y libpcap-dev
# (macOS: brew install libpcap; Windows: install Npcap with the SDK.)
#
# Then, inside your venv:
#     pip install -r requirements-hw.txt

pypcap>=1.3.0          # raw L2 capture for HomePlug AV / V2GTP frames
