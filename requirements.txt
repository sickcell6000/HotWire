# HotWire — DIN 70121 / ISO 15118 EV Charging Attack Testbed
#
# Python >= 3.9 required

# Core runtime
pypcap>=1.3.0         # raw packet capture for real PLC modem (not needed in simulation)
requests>=2.28.0      # REST callback for SoC status

# Optional: Flask web UI (legacy)
Flask>=2.2.0

# GUI (Checkpoint 3)
PyQt6>=6.6.0

# Development / testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-qt>=4.3.0

# Code quality (optional)
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0
